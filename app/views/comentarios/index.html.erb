<p id="notice"><%= notice %></p>

<h1>Comentários</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Conteudo</th>
      <th>Comentavel</th>
      <th>Comentavel type</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @comentarios.each do |comentario| %>
      <tr>
        <td><%= comentario.conteudo %></td>
        <td><%= comentario.comentavel_id %></td>
        <td><%= comentario.comentavel_type %></td>
        <td><%= button_to 'Mostrar', comentario, method: :get, class: "btn btn-default" %></td>
        <td><%= button_to 'Editar', edit_comentario_path(comentario), method: :get, class: "btn btn-default" %></td>
        <td><%= button_to 'Apagar', comentario, method: :delete, data: { confirm: 'Tem certeza?' }, class: "btn btn-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= button_to 'Novo Comentário', new_comentario_path, method: :get, class: "btn btn-primary", remote: true %>
