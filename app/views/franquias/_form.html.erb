<%= form_for(@franquia) do |f| %>
  <% if @franquia.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@franquia.errors.count, "error") %> prohibited this franquia from being saved:</h2>

      <ul>
      <% @franquia.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
